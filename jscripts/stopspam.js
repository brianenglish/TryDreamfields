  <!-- // hide from old browsers
  function stopspam(user,domain,subject) {
  var abc = "<a href=\"mailto:" + user + "@" + domain + "?subject=" + subject + "\">" + user + "@" + domain + "</a>";
  return abc;
  }
  // -->
